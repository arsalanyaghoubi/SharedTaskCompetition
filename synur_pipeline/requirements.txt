openai>=1.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# For local model support (only needed if using --model-path)
# pip install transformers torch accelerate
# transformers>=4.36.0
# torch>=2.0.0
# accelerate>=0.25.0
